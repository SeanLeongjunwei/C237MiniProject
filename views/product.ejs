<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Fitness App</title>
    <style>
        .product-details {
            margin-top: 20px;
        }
        .product-image {
            max-width: 100%;
            height: auto;
        }
        .back-link {
            margin-top: 20px;
        }
    </style>
</head>
<body style="background-color: #C186FF;">
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/images/lee.png" alt="Fitness App Logo" style="height: 70px;">
            </a>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <br>
        <div class="text-center"><h2>Product Details</h2></div>
        <br>
        <% if (product) { %>
            <div class="row product-details">
                <div class="col-md-4">
                    <img src="/images/<%= product.image %>" class="product-image" alt="Product Image">
                </div>
                <div class="col-md-8">
                    <p><b>Product Name:</b> <%= product.productName %></p>
                    <p><b>Price:</b> $<%= product.price %></p>
                    <p><b>Flavours:</b> <%= product.flavours %></p>
                    <p><b>Description:</b> <%= product.details %></p>
                </div>
            </div>
        <% } else { %>
            <p>No product found.</p>
        <% } %>
        <a href="/" class="btn btn-secondary back-link">Back</a>
    </div>
</body>
</html>